{% extends "components/layout_form.html.twig" %}
{% block form %}
	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="btn btn-light rounded-5 m-3">{{ button_label|default('Enregistrer') }}</button>
	{{ form_end(form) }}
{% endblock %}
